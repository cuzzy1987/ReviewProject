<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <ImageView
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:background="@drawable/selector_register_result_bg"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/resultTv"
        android:layout_marginBottom="20dp"
        />


    <TextView
        android:id="@+id/resultTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="注册结果"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:layout_above="@+id/reasonTv"
        android:layout_marginBottom="20dp"
        android:layout_centerHorizontal="true"
        />
    <TextView
        android:id="@+id/reasonTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="失败原因是因为XXXXXXXXXXXXXX"
        android:textSize="16sp"
        android:layout_above="@+id/confirmBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        />
    <Button
        android:id="@+id/confirmBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="注册结果"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:layout_centerInParent="true"
        android:background="@drawable/shape_next_step_bg"
        android:layout_marginStart="25dp"
        android:layout_marginEnd="25dp"
        android:textColor="@android:color/white"
        android:textSize="16dp"
        />

</RelativeLayout>
